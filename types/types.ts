//Author:Minjie (Zoe) Zuo
export type Note = {
    id: string;
    text: string;
    createdAt: string;
};

// Written by Yanxi
// A Task type for local storage usage
export type Task = {
    id: string;
    title: string;
    done: boolean;
};